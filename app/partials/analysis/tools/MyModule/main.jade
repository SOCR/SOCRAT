div(ng-controller="mymoduleMainCtrl as mainArea")
  h4 {{mainArea.title}}
  div.lead.bg-danger(ng-hide="mainArea.dataType == mainArea.DATA_TYPES.FLAT")
    | MyModule module only supports tabular data
  div(ng-hide="mainArea.dataType != mainArea.DATA_TYPES.FLAT")
    p.
      This module is used only for tutorial purposes
    socrat-my-module-dir
