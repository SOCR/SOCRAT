div(ng-controller='getDataMainCtrl')
    h1 Get Data
    br
    p To perform any analysis, you need data to begin with. Choose one of the available methods
      |to get the data into the web-app.
 
    div.gridStyle(ng-grid="gridOptions",collapse="showState.grid")
 
    div.worldBank(collapse="showState.worldBank")
        p This is worldbank api interface div